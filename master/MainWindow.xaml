<Window x:Class="master.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:master"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"></ColumnDefinition>
            <ColumnDefinition Width="100*"></ColumnDefinition>
            <ColumnDefinition Width="100*"></ColumnDefinition>
            <ColumnDefinition Width="100*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" FontSize="20" Text="{Binding String11}">

        </TextBlock>
        <TextBlock Grid.Column="1" FontSize="20" Text="{Binding String22}">

        </TextBlock>
        <Button Grid.Column="2" Command="{Binding ChangeString1Command}"></Button>
        <Button Grid.Column="3" Command="{Binding ChangeString2Command}"></Button>
    </Grid>
</Window>
